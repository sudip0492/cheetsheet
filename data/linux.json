{
  "Navigation": [
    { "cmd": "pwd", "desc": "Print the working directory" },
    { "cmd": "ls", "desc": "List the directory contents" },
    { "cmd": "ls -la", "desc": "List all contents with details" },
    { "cmd": "cd <dir>", "desc": "Change the directory" },
    { "cmd": "cd ~", "desc": "Navigate to the home directory" },
    { "cmd": "cd -", "desc": "Navigate to the previous directory" },
    { "cmd": "tree", "desc": "Display the directory tree" }
  ],
  "File Operations": [
    { "cmd": "cp <src> <dst>", "desc": "Copy a file" },
    { "cmd": "cp -r <dir> <dst>", "desc": "Copy a directory recursively" },
    { "cmd": "mv <src> <dst>", "desc": "Move or rename a file/directory" },
    { "cmd": "rm <file>", "desc": "Delete a file" },
    { "cmd": "rm -rf <dir>", "desc": "Force delete a directory recursively" },
    { "cmd": "touch <file>", "desc": "Create an empty file" },
    { "cmd": "mkdir <dir>", "desc": "Create a directory" },
    { "cmd": "ln -s <src> <dst>", "desc": "Create a symbolic link" },
    { "cmd": "realpath <file>", "desc": "Get the absolute path of a file" },
    { "cmd": "chmod 755 <file>", "desc": "Change file permissions" },
    { "cmd": "chown <user>:<group> <file>", "desc": "Change file owner and group" },
    { "cmd": "tar -czvf <file.tar.gz> <dir>", "desc": "Create a tarball archive" },
    { "cmd": "tar -xzvf <file.tar.gz>", "desc": "Extract a tarball archive" },
    { "cmd": "tail -f <file>", "desc": "Follow file output dynamically" },
    { "cmd": "less <file>", "desc": "View file contents interactively" }
  ],
  "Search": [
    { "cmd": "grep <pattern> <file>", "desc": "Search for a pattern in a file" },
    { "cmd": "grep -r <pattern>", "desc": "Search for a pattern recursively" },
    { "cmd": "find <path> -name <file>", "desc": "Find a file by name" },
    { "cmd": "locate <name>", "desc": "Find a file quickly using the database" },
    { "cmd": "awk '{print}'", "desc": "Process and format text" },
    { "cmd": "sed 's/<old>/<new>/g'", "desc": "Replace text using regular expressions" }
  ],
  "Processes": [
    { "cmd": "ps aux", "desc": "List all running processes (BSD style)" },
    { "cmd": "ps -ef", "desc": "List all running processes (Standard format)" },
    { "cmd": "ps -ax", "desc": "List all processes with terminal attached" },
    { "cmd": "top", "desc": "Monitor processes in real-time" },
    { "cmd": "htop", "desc": "Enhanced real-time process monitor" },
    { "cmd": "kill <pid>", "desc": "Terminate a process by PID" },
    { "cmd": "kill -9 <pid>", "desc": "Force kill a process by PID" },
    { "cmd": "pkill <name>", "desc": "Terminate a process by name" }
  ],
  "Networking": [
    { "cmd": "ping <host>", "desc": "Check network connectivity" },
    { "cmd": "curl <url>", "desc": "Fetch contents of a URL" },
    { "cmd": "wget <url>", "desc": "Download a file from a URL" },
    { "cmd": "ssh <user>@<host>", "desc": "Log in to a remote host via SSH" },
    { "cmd": "scp <src> <dst>", "desc": "Securely copy files over SSH" },
    { "cmd": "rsync -avz <src> <dst>", "desc": "Synchronize files and directories" },
    { "cmd": "ip a", "desc": "Show IP addresses and network interfaces" }
  ],
  "Pipes & Redirection": [
    { "cmd": "cmd1 | cmd2", "desc": "Pass output of cmd1 as input to cmd2 (Pipe)" },
    { "cmd": "ls -la | less", "desc": "Page through a long list of files" },
    { "cmd": "ps aux | grep <name>", "desc": "Find a specific running process" },
    { "cmd": "cmd > <file>", "desc": "Write output to a file (overwrites)" },
    { "cmd": "cmd >> <file>", "desc": "Append output to a file" },
    { "cmd": "cmd > /dev/null 2>&1", "desc": "Run a command and discard all output" }
  ],
  "System Info": [
    { "cmd": "df -h", "desc": "Show free disk space" },
    { "cmd": "du -sh <dir>", "desc": "Show directory disk usage" },
    { "cmd": "free -m", "desc": "Show available memory" },
    { "cmd": "uname -a", "desc": "Show system information" }
  ]
}